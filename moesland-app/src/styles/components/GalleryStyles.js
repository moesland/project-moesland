import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  view: {
    height: '90%',
  },
  container: {
    padding: 8,
  },
  text: {
    margin: 10,
    textAlign: 'center',
  },
});

export default styles;
