import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  photo: {
    width: '100%',
    height: '90%',
    resizeMode: 'contain',
  },
});

export default styles;
