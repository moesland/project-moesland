import { Dimensions, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 8,
  },
  itemContainer: {
    flex: 1,
    margin: 2,
    overflow: 'hidden',
  },
  image: {
    width: '100%',
    height: 150,
  },
});

export default styles;
