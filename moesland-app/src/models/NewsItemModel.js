export default class NewsItemModel {
  constructor(id, date, title, bannerImage, content) {
    this.id = id;
    this.date = date;
    this.title = title;
    this.content = content;
    this.bannerImage = bannerImage;
  }
}