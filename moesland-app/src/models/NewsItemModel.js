export default class NewsItemModel {
  constructor(id, date, title, text, bannerImage) {
    this.id = id;
    this.date = date;
    this.title = title;
    this.text = text;
    this.bannerImage = bannerImage;
  }
}