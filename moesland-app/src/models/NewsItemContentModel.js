export default class NewsItemContentModel {
    constructor(id, text, attributes, image) {
        this.id = id;
        this.text = text;
        this.attributes = attributes;
        this.image = image;
    }
}